apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-service-opcache-jit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: php-service-opcache-jit
  template:
    metadata:
      labels:
        app: php-service-opcache-jit
    spec:
      containers:
        - name: php-fpm
          image: pdp-prep/php-opcache-jit
          ports:
            - containerPort: 9000
              name: php-fpm
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          env:
            - name: PHP_FPM_LISTEN
              value: "0.0.0.0:9000"
---
apiVersion: v1
kind: Service
metadata:
  name: php-service-opcache-jit
spec:
  selector:
    app: php-service-opcache-jit
  ports:
    - port: 9000
      name: php-fpm
      targetPort: 9000
  type: ClusterIP

