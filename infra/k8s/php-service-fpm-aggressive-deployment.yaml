apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-service-fpm-aggressive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: php-service-fpm-aggressive
  template:
    metadata:
      labels:
        app: php-service-fpm-aggressive
    spec:
      containers:
        - name: php-fpm
          image: pdp-prep/php-fpm-aggressive
          ports:
            - containerPort: 9000
              name: php-fpm
          resources:
            requests:
              memory: "512Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          env:
            - name: PHP_FPM_LISTEN
              value: "0.0.0.0:9000"
---
apiVersion: v1
kind: Service
metadata:
  name: php-service-fpm-aggressive
spec:
  selector:
    app: php-service-fpm-aggressive
  ports:
    - port: 9000
      name: php-fpm
      targetPort: 9000
  type: ClusterIP

