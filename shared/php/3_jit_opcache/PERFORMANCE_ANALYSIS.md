## [На главную](../../README.md)
# Анализ производительности OPcache и JIT

## 🔍 Почему результаты отличаются от ожиданий?

### 1. JSON операции: почему no-opcache быстрее?

#### Результаты:
- **no-opcache**: 158ms (общее время), encode: 7.47ms
- **opcache-only**: 251ms (общее время), encode: 7.74ms  
- **opcache-jit**: 253ms (общее время), encode: 13.92ms

#### Причины:

1. **JSON функции - это нативные C функции**
   - `json_encode()` и `json_decode()` реализованы на C, не на PHP
   - Они уже оптимизированы на уровне компилятора C
   - OPcache кэширует **байткод PHP**, но не ускоряет сами нативные функции
   - JIT компилирует **PHP код**, но не может оптимизировать вызовы C функций

2. **Накладные расходы OPcache/JIT**
   - Проверка кэша OPcache
   - Управление JIT буфером (128MB)
   - Инициализация и поддержка структур данных
   - На быстрых операциях (7-13ms) эти накладные расходы заметны

3. **Память и кэширование**
   - no-opcache использует меньше памяти (64.69MB vs 80.05MB)
   - Меньше памяти = меньше работы сборщика мусора
   - Меньше кэш-промахов на уровне CPU

4. **JIT overhead для коротких операций**
   - JIT эффективен для "горячего" кода (много итераций)
   - Для 5 итераций JIT не успевает "прогреться"
   - Накладные расходы на анализ и компиляцию превышают выгоду

#### Вывод:
**JSON операции не выигрывают от OPcache/JIT**, потому что:
- Основная работа выполняется в нативных C функциях
- Накладные расходы на управление кэшем заметны на быстрых операциях
- JIT не успевает оптимизировать короткие циклы

---

### 2. I/O операции: почему нет разницы?

#### Результаты:
Все три конфигурации показывают практически одинаковые результаты:
- Write: ~36-40ms
- Read: ~16-58ms (вариация из-за кэширования ОС)
- Total: ~425-437ms

#### Причины:

1. **I/O операции - это системные вызовы**
   - `file_get_contents()`, `file_put_contents()` → системные вызовы Linux
   - `read()`, `write()` на уровне ядра ОС
   - **Не зависят от PHP кода вообще**

2. **Узкое место - диск, не CPU**
   - Основное время тратится на:
     - Ожидание диска (I/O wait)
     - Копирование данных в/из ядра
     - Работа файловой системы
   - CPU ждёт диск, а не наоборот

3. **Кэширование на уровне ОС**
   - Linux кэширует файлы в RAM (page cache)
   - Второе чтение может быть быстрее из-за кэша ОС
   - Это объясняет вариацию в read операциях

4. **OPcache/JIT не влияют на системные вызовы**
   - Они оптимизируют выполнение PHP кода
   - Но не могут ускорить ожидание диска
   - Не влияют на скорость копирования данных

#### Вывод:
**I/O операции не выигрывают от OPcache/JIT**, потому что:
- Узкое место - диск, а не выполнение PHP кода
- Системные вызовы выполняются на уровне ОС
- Кэширование происходит на уровне ОС, а не PHP

---

## ✅ Где OPcache и JIT действительно помогают?

### 1. Арифметические операции (Fibonacci)
- **Много циклов** - JIT оптимизирует горячие циклы
- **Чистый PHP код** - нет вызовов нативных функций
- **Много итераций** - JIT успевает "прогреться"
- **Ожидаемый результат**: opcache-jit > opcache-only > no-opcache

### 2. Сложная бизнес-логика
- Обработка больших массивов в PHP
- Сложные вычисления
- Рекурсивные алгоритмы
- Обработка строк в PHP (не нативные функции)

### 3. Долгоживущие процессы (PHP-FPM)
- OPcache создаётся один раз и обслуживает тысячи запросов
- JIT накапливает статистику по множеству запросов
- В CLI (как в тестах) эффект меньше


## 🎯 Итоговые выводы

1. **OPcache/JIT не ускоряют нативные C функции** (json_encode, file_get_contents)
2. **OPcache/JIT не ускоряют системные вызовы** (I/O операции)
3. **OPcache/JIT эффективны для чистого PHP кода** с циклами и вычислениями
4. **JIT требует "прогрева"** - нужны сотни/тысячи итераций. JIT имеет "стоимость входа" - анализ и компиляция требуют времени.  Поэтому в функциях, которые выполняются раз в год нет смысла. Эффект проявляется только после N итераций. Для коротких скриптов JIT вреден - накладные расходы не окупаются
5. **В CLI эффект меньше**, чем в PHP-FPM (долгоживущие процессы)

Ваши результаты **корректны** и демонстрируют реальное поведение PHP!

